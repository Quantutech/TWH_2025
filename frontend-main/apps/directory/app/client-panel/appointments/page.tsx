import React from "react";
import Layout from "../../../components/layout/client-panel/Layout";

const page = () => {
  return <Layout header="Appointments">Appointments</Layout>;
};

export default page;
