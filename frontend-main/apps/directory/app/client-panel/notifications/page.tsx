import React from "react";
import Layout from "../../../components/layout/client-panel/Layout";

const page = () => {
  return <Layout header="Notifications">Notifications</Layout>;
};

export default page;
